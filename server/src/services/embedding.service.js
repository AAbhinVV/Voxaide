const embeddingService = async (transcriptionText) => {
    //chunking the text
    const chunks = [];
    

    //creating embeddings for each chunk


    //storing in pinecone


    //storing metadata in mongo

}